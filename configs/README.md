# Configs

Configuration files for reproducible runs belong here. Each training, tuning,
baseline, and plotting command should be traceable to a saved config whose hash
appears in `results/experiments.csv`.
